<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameMagic_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".main.ui.magic.MagicFragment">

    <include
        android:id="@+id/include_activity"
        layout="@layout/content_magic"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <es.upm.etsiinf.dam.coinapp.main.ui.magic.BallView
        android:id="@+id/view_ball"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</FrameLayout>